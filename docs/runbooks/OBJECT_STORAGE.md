# Object Storage

Buckets are private. Uploads use short-lived presigned PUT URLs. Storage keys never include original filenames. Future cleanup jobs will remove orphaned failed uploads and apply retention policies.
