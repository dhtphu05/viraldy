# Job Failure

Inspect `processing_jobs` by workspace and status. Safe errors are stored in `error_code` and `error_message`; stack traces stay in logs. Queued jobs can be redispatched by a future recovery command.
