# Deployment

Deploy API and worker as separate processes from the same backend image. Run migrations first. Production must configure OIDC, non-wildcard CORS, PostgreSQL, Redis, and private S3-compatible storage.
