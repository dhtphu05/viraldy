# Tenant Access Incident

If tenant isolation is suspected, revoke affected tokens, preserve logs with request IDs, identify workspace/resource IDs involved, audit membership rows, and patch authorization checks before restoring access.
